# friction log

| Tried | da-cli command helped | Missing CLI primitive | Manual workaround | Suggested da-cli improvement |
|---|---|---|---|---|
| Preview and publish the full commerce content tree | `da --commit preview tree / --branch main --yes --concurrency 1` can safely serialize the 1029-page tree | Tree preview/publish is inherently slow and earlier higher-concurrency attempts hit `admin.hlx.page` `429` rate limits; foreground commands do not expose checkpointed progress | Ran the bulk preview as a long foreground process and treated live publish as dependent on preview completion | Add durable async tree jobs with state files, path-level progress, 429 backoff, resume, `--changed-only`, and structured output for adjunct agent workers |
| Target the new boilerplate repo after clone | `da config set org`, `da config set repo` | No single command to initialize DA-backed fstab plus `.da.json` from an existing Git remote without `gh` | Added `fstab.yaml` locally and set config with two commands | Add `da site connect --org somarc --repo fluffyjaws-medical --write-fstab` for existing repos, independent of GitHub CLI |
| Dry-run content upload for the authored tree | `da content put-tree content --dry-run --format json` | Upload planner labels JSON as `type: html` and `mimeTypeForUpload()` falls back to `text/html` for `.json` | Kept `metadata.json` in content because DA upload still plans the path, but noted the MIME mismatch before commit | Add JSON/YAML/TXT MIME handling and show MIME in `put-tree` preflight |
| Browser-validate local content through the DA server | `da up --fallback none` | Local content mode served authored source documents without the full EDS decoration shell, so block JS/CSS did not execute in the observed page | Used `curl`, `da pipeline quality-gate`, and browser observe/network checks to validate source rendering; remote preview is still needed for decorated runtime validation | Add a `da up --decorate` or Helix-compatible local transform mode that serves content through the same scripts/styles shell used by preview |
| Browser-console validation through managed runtime | `da up` provided a URL for the browser | Playwright automation endpoint was unavailable in this runtime, limiting scripted DOM/console assertions | Used `fj_browser observe_tab` and `get_tab_network_events`; console tail and network tail were empty | Add a `da validate browser <url>` command that performs browser console/network checks independent of external Playwright setup |
| Fix a header runtime error from authored nav shape | `da preview tree / --verify` validated non-empty preview, but browser console found the issue | CLI quality gates did not catch the boilerplate header contract requiring separate brand, sections, and tools nav sections | Changed `content/nav.html` to three top-level authored sections and will re-preview `/nav` plus `/index` | Add a header/nav contract audit that checks `/nav.plain.html` shape for boilerplate compatibility |
| Resolve remaining header error after nav shape fix | Browser-console validation on preview found `brandLink.closest('.button-container')` was null because current button decoration uses `.button-wrapper` | No CLI/code audit identifies mismatches between boilerplate block assumptions and current global decoration output | Patched `blocks/header/header.js` to accept `.button-container` or `.button-wrapper` and guard missing brand elements | Add a `da code audit boilerplate-contracts` check for known block/global decoration contract drift |
| Diagnose broken contact page against a known-valid DA demo | `da content list`, `da content tree`, and `da content get` allowed read-only inspection of `scdemos/demo` into an isolated local folder | No CLI primitive explains a block's expected DA source shape or finds a valid example across a repo | Copied FIRE demo form JSON, contact fragment, and form docs locally under `fire-demo-inspect/` and compared the authored block shape manually | Add `da block inspect form --examples` or `da content examples --block form` to show valid source structure, required companion sheets, and endpoint rows |
| Investigate DA source versus preview stripping the contact block class | `da content get /contact.html` showed the source retained the class, while preview plain HTML stripped it | CLI did not surface why the preview pipeline dropped/flattened the authored block class | Compared against FIRE demo and found valid forms use `div.form` with a link to a sheet JSON plus an action endpoint, not row-per-field HTML | Add a preview/source reconciliation diagnostic that highlights removed block classes and likely authoring contract violations |
| Understand DA project-level sheet resources for forms | `da block inspect form` explained the page block contract, and `da content get /forms/contact-us.json` exposed the sheet payload | No command directly listed DA sheets/config JSON such as `/forms/contact-us.json` with columns/fields, so agents had to know where to look | Added local `/forms/intake.json`, a `form` block, and tested a da-cli `content sheets /forms` enhancement against FIRE | Add `da content sheets [prefix]` and make `put-tree` show JSON MIME/kind so sheet-backed content is visible in plans |
| Preserve block classes in preview after switching to sheet-backed form | `da preview explain /contact` quickly showed `form`, `fragment`, and `intake` were still removed from `.plain.html` | Block contract diagnostics did not explain that block tables must be nested inside a section wrapper rather than authored as the top-level section div | Moved the `form` and `fragment` blocks inside the opening content section wrapper, then re-ran preview explain until removed classes was empty | Extend `da preview explain` or `da block inspect` with a section-wrapper hint when a source block class appears on a top-level `<main>` child and disappears in `.plain.html` |
| Validate sheet-backed form runtime in browser | `da preview page /contact.html`, `da preview explain /contact`, and `da content sheets /forms` verified the page and sheet source shape | `da preview tree / --verify` only previews HTML, so the linked `/forms/intake.json` route was not built and browser fetches returned 404 | Ran `da preview page /forms/intake.json` explicitly, then reloaded the contact page and checked browser console/network | Add a pipeline helper or contract hint that expands sheet-backed block dependencies into explicit preview steps, e.g. `da preview dependencies /contact` or `da preview tree --include-sheets` |
| Validate global runtime primitives locally | `da up --fallback none` served local content and code for smoke checks | Local `da up` content serving did not behave like decorated EDS preview for header/global runtime, so nav search and theme needed remote preview validation | Used local lint/quality gates first, then DA upload plus AEM preview for real browser validation | Add `da up --decorate` or a `da validate runtime` command that runs the same shell/decorator flow as preview |
| Validate new block JS/CSS on preview | `da content put-tree` and `da preview page` updated content quickly | DA upload and preview do not update codebus assets; new blocks and header runtime are invisible until GitHub push/AEM sync updates code | Pushed the Git commit, waited for AEM sync, then reopened preview with cache-busting | Add `da site code-status` or `da preview wait-code --commit <sha>` to confirm preview is serving the expected Git SHA before browser validation |
| Explain localized folder index page | `da preview explain /es/index` correctly showed `cards` survived in `.plain.html` | `da preview explain /es/` built `/es/.plain.html` and falsely reported removed classes | Re-ran the diagnostic with `/es/index` | Normalize folder-index plain URLs in `preview explain` so `/es/` maps to `/es/index.plain.html` |
| Browser-validate fictional login page | `da preview explain /login`, preview quality gate, and `site reconcile /login` validated source and audit health | Managed browser treated `/login` as a real sign-in surface and blocked observation as user login-required | Verified `/login.plain.html` and block JS by direct HTTP; did not perform scripted login interaction | Add an agent/browser validation mode or `da validate browser --allow-demo-login` that can test fictional login blocks without triggering auth handoff |
| Script DOM interactions for quiz/search/theme | Browser observe confirmed rendered controls and console/network health | Managed Playwright endpoint was unavailable and click/type interactions returned runtime errors, so assertions could not be fully automated | Used observe snapshots, network/console tails, curl checks, lint, preview explain, quality gates, and reconcile outputs | Add `da validate browser <url> --checks nav-search,theme,quiz,login` independent of external Playwright availability |
| Add a video hero backed by a local MP4 asset | `da content put-tree content --dry-run --format json` correctly identified `/media/fluffyjaws-medical.mp4` as `kind: binary` with `video/mp4` | Previewing the page did not make the linked media route available; the MP4 stayed unavailable until previewed explicitly | Ran `da preview page /media/fluffyjaws-medical.mp4 --format json`, then verified the media URL redirected to the hashed MP4 | Add `da preview dependencies /path` or `da preview tree --include-assets` so linked media, sheets, and fragments are previewed with the page |
| Reclassify splash video as codebus asset | `da code --help` exposed the code-bus surface for sync/status/purge | The earlier content-first workflow made it too easy to treat a global splash MP4 as DA content media instead of a code asset | Moved the MP4 to `videos/fluffyjaws-medical.mp4`, repointed the hero to `/videos/fluffyjaws-medical.mp4`, and removed the tracked content-media copy | Add a `da block inspect hero --variant video` or authoring hint that distinguishes content-owned inline media from site-shell/splash assets that should ride codebus |
| Align splash video with the proven `fluffyjaws-eds` pattern | `da content put /index.html content/index.html --dry-run --format json` made the remaining source delta explicit | The prior correction still used a made-up `/videos/` code path and overloaded the generic `hero` block instead of following the existing `media/` plus `video-hero` implementation pattern | Compared the local `fluffyjaws-eds` repo, ported `blocks/video-hero`, moved the MP4 to root `media/fluffyjaws-medical.mp4`, and changed the homepage block to `video-hero` | Add a DA CLI/site primitive that can discover existing local block/media conventions across sibling EDS repos or codify “implementation media lives in root `media/` and uses block-specific code” in `block inspect` docs |
| Sync the new video hero block code | `da code sync /blocks/video-hero/video-hero.js` and `da code sync /blocks/video-hero/video-hero.css` synced the exact codebus resources | `da code sync /blocks/video-hero` treated the directory as a single resource and returned a 404 instead of recursively syncing block assets or suggesting file paths | Re-ran code sync separately for the JS and CSS files | Make `da code sync` accept directories recursively, or emit a targeted hint like “sync /blocks/video-hero/video-hero.js and .css” when the path is a block folder |
| Clear stale media-route ownership after moving from DA media to codebus media | `da code status /media/fluffyjaws-medical.mp4` showed the codebus asset was healthy, and `da content get /media/fluffyjaws-medical.mp4` showed DA source was gone | The public `.aem.page` route still served the previous DA media redirect until explicitly purged, which masked the newly synced codebus asset at the same path | Ran `da --commit code purge /media/fluffyjaws-medical.mp4`, after which the route returned direct `200 video/mp4` | Add a warning when a path previously existed in DA content media and is later synced as codebus media, with an automatic or suggested purge step |
| Validate homepage after adding the video hero | `da preview explain /index --format json` caught that `cards` disappeared from `.plain.html` while the new `hero video` classes survived | The command reported class removal but did not explain the likely top-level-section wrapper cause; the same authoring pitfall already appeared with forms | Moved the `cards` block inside the surrounding content section wrapper, re-uploaded `/index.html`, waited for `da preview page`, then re-ran `preview explain` until clean | Generalize the section-wrapper diagnostic across all blocks, not only forms; flag source classes on top-level `<main>` children before upload |
| Browser-check the homepage video runtime | `da preview page /index.html` and `da preview explain /index` confirmed the page and block classes; browser observe confirmed rendered homepage content and no failed network snapshot | Managed Playwright/CDP endpoint was unavailable for a precise DOM assertion that `.hero video` loaded and autoplayed | Used browser observation plus network snapshot and direct media HEAD checks; recorded the missing scripted assertion as residual validation friction | Add `da validate browser <url> --assert '.hero video' --network-ok` or a built-in video/media block validation check |
